/*
 * WordCountReducer.cpp
 *
 *  Created on: 2023. 11. 22.
 *      Author: sehyeon
 */

#import <Reducer.h>
#include <algorithm>

class WordCountReducer : public Reducer {
public:
	void reducer(const std::string& key, const std::vector<int>& values, int& output) {
		output = std::accumulate(values.begin(), values.end(), 0);
	}
};
